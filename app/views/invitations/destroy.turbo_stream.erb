<%= turbo_stream.update "suggested_friend_#{@friend.id}" do %>
    <%= render "shared/add_friend_button", friend: @friend %>
<% end %>

<%= turbo_stream.replace "pending-counter" do %>
    <%= render "shared/pending_counter" %>
<% end %>

<%= turbo_stream.remove "invitation_#{@invitation.id}" %>