<div id="like-region-<%= "#{likeable.class.name.downcase}-#{likeable.id}" %>">
    <% if Like.like_exists?(likeable, current_user) %>
        <%= render "likes/unlike", likeable: likeable %>
    <% else %>
        <%= render "likes/like", likeable: likeable %>
    <% end %>
</div>