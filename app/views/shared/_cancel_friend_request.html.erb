<%= form_with model: Invitation.find_invitation(current_user.id, friend.id).take, method: :delete do |form| %>
    <%= form.submit "Cancel request", class: "h-full p-2 hover:bg-red-200 focus:bg-red-100 rounded-md hover:cursor-pointer" %>
<% end %>