<div id="post_<%= post.id %>" class="flex flex-col gap-2 border-2 border-white w-full p-2 bg-red-50 rounded-md">
    <%# There will be pfp here later %>
    <div class="px-2">
        <p class="text-base font-semibold"><%= "#{post.author.first_name} #{post.author.last_name}" %></p>
        <p class="text-sm text-slate-500"><%= "#{time_ago_in_words(post.created_at)} ago" %></p>
    </div>
    
    <div>
        <%= post.body %>
    </div>

    <div class="flex border-t border-b border-slate-200 py-2">
        <% if post.author == current_user %>
            <%= button_to post_path(post), method: :delete, class: "flex items-center self-center px-3 py-1 rounded-full hover:bg-red-100 focus:bg-red-200" do %>
                Remove
                <svg class="w-6 h-6" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><title>trash-can-outline</title><path d="M9,3V4H4V6H5V19A2,2 0 0,0 7,21H17A2,2 0 0,0 19,19V6H20V4H15V3H9M7,6H17V19H7V6M9,8V17H11V8H9M13,8V17H15V8H13Z" /></svg>
            <% end %>
        <% end %>
    </div>
    
    <div>
        <%= render "comments/comment_form", post: post %>
    </div>

    <%# Post ID is needed to target correct Post with comment via turbo streams %>
    <div id="comments_<%= post.id %>" class="divide-y border-t border-slate-400 pt-2">
        <%= render post.comments.order(created_at: :desc) %>
    </div>
</div>

